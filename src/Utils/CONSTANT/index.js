const GEO_WATCH = {
    accuracy: 'high',
    enableHighAccuracy: true,
    distanceFilter: .5,
    interval: 500,
    fastestInterval: 1000,
    forceRequestLocation: true,
}

const FENCING_RADIUS = 5;

export {
    FENCING_RADIUS,
    GEO_WATCH,
}